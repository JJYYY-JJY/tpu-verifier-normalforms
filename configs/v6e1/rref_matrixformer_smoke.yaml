task: rref_matrixformer_smoke
profile:
  name: local-rref-matrixformer-smoke
data:
  trace_schema: rref-backward-trace-npz-v1
  state_schema: rref-state-action-npz-v1
  format: zarr
  count: 8
  seed_start: 0
  max_backward_ops: 8
field:
  modulus: 101
matrix:
  family: dense
  rows: 16
  cols: 16
model:
  name: rref-matrixformer
  row_embedding_dim: 8
  col_embedding_dim: 8
  hidden_dim: 32
  layers: 1
  num_heads: 1
train:
  steps: 10
  batch_size: 4
  seed: 0
rollout:
  command: rref-verifier-beam
  max_steps: 16
  beam_width: 4
  batch_size: auto
success:
  require_finite_loss: true
  require_checkpoint_beam: true
  require_explicit_failure_status: true
artifacts:
  work_dir: /tmp/nf-v6e1/rref_matrixformer_smoke/work
  report_dir: /tmp/nf-v6e1/rref_matrixformer_smoke/report
  commit_policy: compact_reports_only
